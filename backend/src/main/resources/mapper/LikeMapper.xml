<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.raisetimeline.api.like.LikeMapper">

    <insert id="insert">
        INSERT INTO likes (post_id, user_id)
        VALUES (#{postId}, #{userId})
        ON CONFLICT DO NOTHING
    </insert>

    <delete id="delete">
        DELETE FROM likes WHERE post_id = #{postId} AND user_id = #{userId}
    </delete>

    <select id="exists" resultType="boolean">
        SELECT EXISTS(
            SELECT 1 FROM likes WHERE post_id = #{postId} AND user_id = #{userId}
        )
    </select>

    <select id="countByPostIds" resultType="com.raisetimeline.api.like.PostCount">
        SELECT post_id, COUNT(*) AS cnt
        FROM likes
        WHERE post_id IN
        <foreach item="id" collection="postIds" open="(" separator="," close=")">
            #{id}
        </foreach>
        GROUP BY post_id
    </select>

    <select id="likedPostIdsByUser" resultType="long">
        SELECT post_id
        FROM likes
        WHERE user_id = #{userId}
        AND post_id IN
        <foreach item="id" collection="postIds" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

</mapper>
